apiVersion: v1
kind: Service
metadata:
  name: django-service
  namespace: django-namespace
spec:
  selector:
    app: django-app
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30008  # You can modify this if needed
  type: NodePort
